<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>我是谁</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{who}}" wx:key >
              <text data-id='{{item.va}}' bindtap='whoTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>我多大了</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{big}}" wx:key >
              <text data-id='{{item.va}}' bindtap='bigTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>工作状态</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{work}}" wx:key >
              <text data-id='{{item.va}}' bindtap='workTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>养宠经验</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{yan}}" wx:key >
              <text data-id='{{item.va}}' bindtap='yanTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>婚姻状况</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{jie}}" wx:key >
              <text data-id='{{item.va}}' bindtap='jieTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>住房情况</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{zhu}}" wx:key >
              <text data-id='{{item.va}}' bindtap='zhuTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group'>
    <view class='a-text'>我的月收入</view>
    <view class='a-btn'>
        <view class="flex flex-wrap">
          <view class="basis-xs  radius a-btn-t" wx:for="{{money}}" wx:key >
              <text data-id='{{item.va}}' bindtap='moneyTap' class='{{item.checked?"text-orange":""}}'>{{item.va}}</text>
          </view>
        </view>
    </view>
  </view>
</view>
<view class="a-body">
  <view class='a-group-1'>
    <view class='a-text'>申请自述</view>
    <view class='a-btn'>
        <textarea maxlength="-1" class='a-btn-te' bindinput="textareaBInput" placeholder="请诉说TA的故事"></textarea>
    </view>
  </view>
</view>
<view class="padding flex flex-direction bg-white" style='padding-top: 20px;'>
  <button class="cu-btn bg-red margin-tb-sm lg" bindtap='tbtn'>提交</button>
</view>



